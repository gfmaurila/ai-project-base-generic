# Docker
Quando Docker fizer parte do projeto:
- manter configuração reproduzível;
- usar `.env.example`;
- healthchecks quando aplicável;
- volumes nomeados para persistência;
- não armazenar segredos na imagem.
